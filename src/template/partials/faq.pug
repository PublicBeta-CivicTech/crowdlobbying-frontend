section.faq
  +stripe()(viewportClass='vertical-space')
    .richtext.readable-width
      h1 #{faq.title}
      each item in faq.content
        if item.question
          h2 #{item.question}
        p #{item.answer}
